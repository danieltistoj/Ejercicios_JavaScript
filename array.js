const array =[];
array[0]= "1";
array[1]="2";

const array2 = new Array(1000);

const obj = {
    unArray:new Array(1000)
};

let longitud = obj.unArray.length;
for(let i=0, longitud= obj.unArray.length;i<longitud;i++){
    obj.unArray[i]='hola';
}